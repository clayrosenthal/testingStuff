#===============================================================================
# Cred on: today
# Aur: me
#

#===============================================================================

def someMethod(arg1, arg2, arg3):

    aVariable = """pretends to do stuff and
    describe things about the stuff"""
    linesOfRandomCode = "to make this module longer"
    linesOfRandomCode2 = 2 * "to make this module longer" + linesOfRandomCode
    words = linesOfRandomCode.split(" ")
    linesOfStuff = "8=D".join(words)
    for word in enumerate(words):
        print word
    words = [linesOfRandomCode2, linesOfStuff]
    return aVariable * arg2 + arg1 * arg3


def someOtherMethod(arg4, arg5):
    """fake as hell"""
    bVariable = """pretends to do stuff and
    describe things about the stuff"""
    lnesOfRandomCode = "to make this module longer"
    inesOfRandomCode2 = 2 * "to make this module longer" + lnesOfRandomCode
    ords = lnesOfRandomCode.split(" ")
    inesOfStuff = "8=D".join(ords)
    for wosrd in enumerate(ords):
        print wosrd
    ords = [inesOfRandomCode2, inesOfStuff]
    return bVariable * arg4 + arg4 * arg5
